{
  "name": "uwo-user-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "UWO Smart Referral & Attribution User Dashboard",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview",
    "build:docker": "docker build -t uwo-user-dashboard .",
    "deploy:cloudrun": "powershell -ExecutionPolicy Bypass -File ./deploy-cloudrun.ps1"
  }
}
