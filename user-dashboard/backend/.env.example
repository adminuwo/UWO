PORT=5000
MONGODB_URI=mongodb://localhost:27017/referral_db
JWT_SECRET=supersecret_referral_jwt_key_982341
FRONTEND_URL=http://localhost:5173

# Email Delivery Configuration (Nodemailer)
# Set SMTP_ENABLE=true once you enter your SMTP credentials.
# If SMTP_ENABLE=false, the system will log generated credentials to the server console.
SMTP_ENABLE=false
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM="Referral Portal" <noreply@yourdomain.com>
