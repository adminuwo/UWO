version: '3.8'

services:
  unified_dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: unified_dashboard_app
    restart: always
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - BACKEND_API_URL=http://localhost:8080
